# 可安全开源清理清单（最小版）

## 已完成（本次已改）

- [x] 移除 `Vcode.java` 中硬编码阿里云短信密钥，改为环境变量读取：
  - `ALIBABA_CLOUD_ACCESS_KEY_ID`
  - `ALIBABA_CLOUD_ACCESS_KEY_SECRET`
- [x] 移除 `Vcode.java` 中硬编码 SMTP 授权信息，改为环境变量读取：
  - `SMTP_USERNAME`
  - `SMTP_APP_PASSWORD`
- [x] 移除 `Vcode.java` 中固定手机号，改为使用请求参数号码。
- [x] 掩码历史注释中的密钥示例（`<REDACTED_...>`）。
- [x] 登录“记住密码”改为仅记住用户名，不再写入明文密码 Cookie。
- [x] 管理端与前台用户资料页不再回填/展示明文密码（改为密码框空值输入）。
- [x] 用户列表页密码列改为脱敏显示（`******`）。
- [x] 兼容“留空不改密码”：更新用户信息时密码为空则沿用旧密码。

## 你还需要手工完成（发布前必做）

- [ ] 在云平台和邮箱后台立即轮换（Rotate）旧密钥和授权码。
- [ ] 在部署环境中配置上述环境变量，验证短信/邮件发送功能。
- [ ] 确认 `.gitignore` 已忽略：
  - `.idea/`、`.vs/`、`target/`、`*.iml`、日志文件、私有配置文件。
- [ ] 二次检查是否存在数据库导出文件、测试账号、真实手机号/邮箱等隐私数据。

## 推荐后续增强（非本次最小改造范围）

- [ ] 密码统一使用强哈希（如 BCrypt），禁止明文存储与明文比对。
- [ ] 登录 Cookie 增加 `HttpOnly`、`Secure`、`SameSite` 等安全属性。
- [ ] 增加敏感信息扫描流程（如 Gitleaks/TruffleHog）并接入 CI。

